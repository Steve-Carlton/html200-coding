// Goal:
// Use one loop to console log a variable number, decrease it by one, and log that number, and stop after the number prints 0;

let x = 3;

for (let x = 3; x >= 0; x--) {
   console.log(x);
}
